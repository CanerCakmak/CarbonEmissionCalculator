
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@model IList<ElectricityCalculation>
<div class="content-wrapper">
    <!-- Content -->

    <div class="container-xxl flex-grow-1 container-p-y">
        <h4 class="fw-bold py-3 mb-4">
            <span class="text-muted fw-light">Electricity </span>
            <a style="float:right;" class="btn btn-outline-primary" asp-area="Calculation" asp-controller="Electricity" asp-action="Create">+Veri Ekle</a>
        </h4>

        <!-- Basic Bootstrap Table -->
        <div class="card">
            <h5 class="card-header">Veriler</h5>
            <div class="table-responsive text-nowrap">
                <table class="table">
                    <thead>
                        <tr>
                            <th>Firm Name</th>
                            <th>Country</th>
                            <th>ConsumptionAmount</th>
                            <th>EmissionFactor</th>
                            <th>TotalEmission</th>
                        </tr>
                    </thead>
                    <tbody class="table-border-bottom-0">
                        @if (Model.Any())
                        {


                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>@item.FirmName</td>
                                    <td>@item.Country</td>
                                    <td>@item.ConsumptionAmount</td>
                                    <td>@item.EmissionFactor</td>
                                    <td>@item.TotalEmission</td>
                                    <td>
                                        <a asp-area="Calculation" asp-controller="Electricity" asp-action="Detail" asp-route-id="@item.Id" class="btn p-0">
                                            <i class="bx bx-x"></i>
                                        </a>
                                        <a asp-area="Calculation" asp-controller="Electricity" asp-action="Delete" asp-route-id="@item.Id" class="btn p-0">
                                            <i class="bx bx-x"></i>
                                        </a>
                                    </td>
                                </tr>
                            }
                        }
                        else
                        {
                            <tr>
                                <td>
                                    Veri Bulunamadı
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>

    </div>
</div>

